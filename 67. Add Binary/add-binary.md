# Add Binary (LeetCode 67)

**Difficulty:** Easy · **Language:** Java · **Tests:** 22/22 · **Runtime:** 31ms

---

## Solution

See [`AddBinary.java`](./AddBinary.java) for the full solution.

## Complexity

| | |
|---|---|
| **Time**  | O(n) |
| **Space** | O(1) |

## Coach Notes (from Index 0 session)

> Nice - moving on to the next stage.

Before writing any code, what's your first instinct for how you'd solve this? It doesn't need to be efficient, just a starting angle.

> You're on the right track! 

The code you wrote is a good starting point for understanding how to convert strings to binary.  However, you'll need to handle the cases where the strings are different lengths.

Let's turn that idea into code. Write the most straightforward version you can in the editor, even if it's slow.

> ADVANCE: yes 
Time: O(n)
Space: O(1) 

You're on the right track! 

The code you wrote is a good starting point for understanding how to convert strings to binary.  However, you'll need to handle the cases where the strings are different lengths.  

Let's turn that idea into code. Write the most straightforward version you can in the editor, even if it's slow.

> ADVANCE: yes 
Time: O(n)
Space: O(1) 

You're on the right track! 

The code you wrote is a good starting point for understanding how to convert strings to binary.  However, you'll need to handle the cases where the strings are different lengths.  

Let's turn that idea into code. Write the most straightforward version you can in the editor, even if it's slow.

---

*Auto-documented from Index 0 — 2026-09-20*
